[package]
name = "sqlitex_type_inference"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "type inference for sqlitex"

[dependencies]
sqlparser = {version = "0.59.0", features = ["visitor"]}

[dev-dependencies]
pretty_assertions = "1.4.1"
