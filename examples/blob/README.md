1. Download the `cat.png` and place it in the directory where u will run `cargo run`.

2. the database would persist after running so you can optionally view the image if you are using the VSCode sqlite extension or a 3rd party sqlite viewer.